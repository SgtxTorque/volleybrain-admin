import { View } from 'react-native';

// This is a placeholder screen - the Menu tab opens the drawer instead
// This file is needed to prevent expo-router errors
export default function MenuPlaceholder() {
  return <View />;
}
